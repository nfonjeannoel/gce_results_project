<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for API 31+ -->
    <style name="Theme.GCEResults" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_navbar</item>
        <item name="colorPrimaryVariant">@color/blue_notification_bar</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/blue_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen Theme for API 31+ -->
    <style name="Theme.GCEResults.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background with full image -->
        <item name="windowSplashScreenBackground">@drawable/splash_background</item>
        <!-- Remove the icon to show full background image -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item> -->
        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.GCEResults</item>
    </style>
</resources> 